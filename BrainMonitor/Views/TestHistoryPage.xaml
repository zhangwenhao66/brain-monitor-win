<UserControl x:Class="BrainMirror.Views.TestHistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:converters="clr-namespace:BrainMirror.Converters"
    Background="#FAFAFA">
    
    <UserControl.Resources>
        <converters:DateTimeConverter x:Key="DateTimeConverter" />
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 历史记录显示区域 -->
        <Border Style="{StaticResource CardStyle}" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 区域标题 -->
                <TextBlock Text="历史记录"
                    FontSize="18"
                    FontWeight="SemiBold"
                    Margin="0,0,0,15"
                    Foreground="#2C3E50"
                    HorizontalAlignment="Center" />

                <!-- 历史记录列表 -->
                <DataGrid x:Name="HistoryDataGrid" Grid.Row="1"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="False"
                          CanUserResizeColumns="False"
                          CanUserResizeRows="False"
                          CanUserSortColumns="True"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          SelectionUnit="FullRow"
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeaderWidth="0">
                    
                    <DataGrid.Resources>
                        <!-- 现代化表格样式 -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8F9FA" />
                            <Setter Property="Foreground" Value="#495057" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="Padding" Value="16,12" />
                            <Setter Property="BorderBrush" Value="#E0E0E0" />
                            <Setter Property="BorderThickness" Value="0,0,1,1" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridColumnHeader">
                                        <Border Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="16,12" />
                            <Setter Property="BorderBrush" Value="#E0E0E0" />
                            <Setter Property="BorderThickness" Value="0,0,1,0" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="HorizontalContentAlignment" Value="Center" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="Black" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" 
                                                Padding="{TemplateBinding Padding}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <ContentPresenter VerticalAlignment="Center" 
                                                            HorizontalAlignment="Center" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="Margin" Value="0,2" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridRow">
                                        <Border x:Name="RowBorder" 
                                                Background="{TemplateBinding Background}"
                                                Margin="0,2">
                                            <DataGridCellsPresenter />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="RowBorder" Property="Background" Value="#F8F9FA" />
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="RowBorder" Property="Background" Value="#BBDEFB" />
                                            </Trigger>
                                            <MultiTrigger>
                                                <MultiTrigger.Conditions>
                                                    <Condition Property="IsMouseOver" Value="True" />
                                                    <Condition Property="IsSelected" Value="True" />
                                                </MultiTrigger.Conditions>
                                                <Setter TargetName="RowBorder" Property="Background" Value="#90CAF9" />
                                            </MultiTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="测试时间" Binding="{Binding TestStartTime, Converter={StaticResource DateTimeConverter}}" Width="200" />
                        <DataGridTextColumn Header="AD风险值" Binding="{Binding AdRiskValue, StringFormat={}{0:F2}}" Width="100" />
                        <DataGridTextColumn Header="MoCA评分" Binding="{Binding MocaScore, StringFormat={}{0:F1}}" Width="100" />
                        <DataGridTextColumn Header="MMSE评分" Binding="{Binding MmseScore, StringFormat={}{0:F1}}" Width="100" />
                        <DataGridTextColumn Header="握力值" Binding="{Binding GripStrength, StringFormat={}{0:F1}}" Width="100" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- 主要操作按钮 -->
        <Grid Grid.Row="2" Margin="0,15,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button x:Name="ReturnButton"
                Grid.Column="1"
                Width="120"
                Style="{StaticResource SecondaryButton}"
                Click="ReturnButton_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon Glyph="&#xE72B;" 
                            FontSize="16" 
                            Margin="0,0,8,0"/>
                        <TextBlock Text="返回"/>
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button x:Name="OpenReportButton"
                Grid.Column="3"
                Width="120"
                Style="{StaticResource ModernButton}"
                IsEnabled="False"
                Click="OpenReportButton_Click">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <ui:FontIcon Glyph="&#xE8A5;" 
                            FontSize="16" 
                            Margin="0,0,8,0"/>
                        <TextBlock Text="打开报告"/>
                    </StackPanel>
                </Button.Content>
            </Button>

            <Button x:Name="EnterTestButton"
                Content="点击进入脑电测试"
                Grid.Column="5"
                Width="160"
                Style="{StaticResource ModernButton}"
                Click="EnterTestButton_Click" />
        </Grid>

        <!-- 底部导航按钮
        <StackPanel Grid.Row="3"
            Orientation="Horizontal"
            HorizontalAlignment="Center">
            <Button Content="测试者分组"
                Style="{StaticResource SecondaryButton}"
                Margin="0,0,10,0"
                Click="TesterGroupButton_Click" />
            <Button Content="风险等级"
                Style="{StaticResource SecondaryButton}"
                Margin="0,0,10,0"
                Click="RiskLevelButton_Click" />
            <Button Content="我的"
                Style="{StaticResource SecondaryButton}"
                Click="MyProfileButton_Click" />
        </StackPanel> -->
    </Grid>
</UserControl>