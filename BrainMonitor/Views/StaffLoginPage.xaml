<UserControl x:Class="BrainMirror.Views.StaffLoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             Background="{DynamicResource ApplicationPageBackgroundThemeBrush}">
    
    <UserControl.Resources>
        <!-- 现代Tab样式 -->
        <Style x:Key="ModernTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="450"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Tab头部区域 -->
                            <Border Grid.Row="0" 
                                    Background="#F5F5F5" 
                                    CornerRadius="12,12,0,0"
                                    Margin="0,0,0,0">
                                <TabPanel x:Name="HeaderPanel" 
                                         IsItemsHost="True" 
                                         Background="Transparent"
                                         HorizontalAlignment="Center"
                                         Margin="0"/>
                            </Border>
                            
                            <!-- Tab内容区域 - 固定高度 -->
                            <Border Grid.Row="1" 
                                    Background="White" 
                                    CornerRadius="0,0,12,12"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1,0,1,1"
                                    Height="450">
                                <ContentPresenter x:Name="PART_SelectedContentHost" 
                                                 ContentSource="SelectedContent"
                                                 VerticalAlignment="Top"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 现代Tab项样式 -->
        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="Transparent"
                                BorderThickness="0,0,0,3"
                                CornerRadius="8,8,0,0"
                                Margin="4,4,4,0">
                            <ContentPresenter x:Name="ContentSite"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            ContentSource="Header"
                                            Margin="20,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="White"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="40">
        <!-- 标题区域 -->
        <TextBlock x:Name="TitleText"
                   Text="工作人员登录"
                   FontSize="24"
                   FontWeight="Bold"
                   Foreground="#0078D4"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   Margin="0,0,0,30" />
        
        <Border Style="{StaticResource CardStyle}" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Width="420">
            <StackPanel>
                <!-- Tab控件 -->
                <TabControl x:Name="LoginRegisterTabControl" 
                           Margin="0,0,0,30"
                           Style="{StaticResource ModernTabControl}">
                    
                    <!-- 登录Tab -->
                    <TabItem Header="登录" x:Name="LoginTab" Style="{StaticResource ModernTabItem}">
                        <StackPanel Margin="30,30,30,20">
                            <!-- 账号输入 -->
                            <StackPanel Margin="0,0,0,25">
                                <TextBlock Text="账号" Style="{StaticResource ModernLabel}"/>
                                <TextBox x:Name="LoginAccountTextBox" 
                                         Style="{StaticResource ModernTextBox}"
                                         ui:ControlHelper.PlaceholderText="请输入账号"/>
                            </StackPanel>
                            
                            <!-- 密码输入 -->
                            <StackPanel Margin="0,0,0,35">
                                <TextBlock Text="密码" Style="{StaticResource ModernLabel}"/>
                                <PasswordBox x:Name="LoginPasswordBox" 
                                             Style="{StaticResource ModernPasswordBox}"
                                             ui:ControlHelper.PlaceholderText="请输入密码"/>
                            </StackPanel>
                            
                            <!-- 按钮区域 -->
                            <Grid Margin="0,15,0,25" HorizontalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 返回按钮 -->
                                <Button Grid.Column="0"
                                        Style="{StaticResource SecondaryButton}"
                                        Click="BackButton_Click"
                                        Width="120">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <ui:FontIcon Glyph="&#xE72B;" 
                                                FontSize="16" 
                                                Margin="0,0,8,0"/>
                                            <TextBlock Text="返回"/>
                                        </StackPanel>
                                    </Button.Content>
                                </Button>
                                
                                <!-- 登录按钮 -->
                                <Button Grid.Column="2"
                                        x:Name="LoginButton"
                                        Content="登录" 
                                        Style="{StaticResource ModernButton}"
                                        Click="LoginButton_Click"
                                        Width="120"/>
                            </Grid>
                        </StackPanel>
                    </TabItem>
                    
                    <!-- 注册Tab -->
                    <TabItem Header="注册" x:Name="RegisterTab" Style="{StaticResource ModernTabItem}">
                        <StackPanel Margin="30,25,30,15">
                            <!-- 姓名输入 -->
                            <StackPanel Margin="0,0,0,18">
                                <TextBlock Text="姓名" Style="{StaticResource ModernLabel}"/>
                                <TextBox x:Name="RegisterNameTextBox" 
                                         Style="{StaticResource ModernTextBox}"
                                         ui:ControlHelper.PlaceholderText="请输入姓名"/>
                            </StackPanel>
                            
                            <!-- 工号输入 -->
                            <StackPanel Margin="0,0,0,18">
                                <TextBlock Text="工号" Style="{StaticResource ModernLabel}"/>
                                <TextBox x:Name="RegisterStaffIdTextBox" 
                                         Style="{StaticResource ModernTextBox}"
                                         ui:ControlHelper.PlaceholderText="请输入工号"/>
                            </StackPanel>
                            
                            <!-- 账号输入 -->
                            <StackPanel Margin="0,0,0,18">
                                <TextBlock Text="账号" Style="{StaticResource ModernLabel}"/>
                                <TextBox x:Name="RegisterAccountTextBox" 
                                         Style="{StaticResource ModernTextBox}"
                                         ui:ControlHelper.PlaceholderText="请输入账号"/>
                            </StackPanel>
                            
                            <!-- 密码输入 -->
                            <StackPanel Margin="0,0,0,25">
                                <TextBlock Text="密码" Style="{StaticResource ModernLabel}"/>
                                <PasswordBox x:Name="RegisterPasswordBox" 
                                             Style="{StaticResource ModernPasswordBox}"
                                             ui:ControlHelper.PlaceholderText="请输入密码"/>
                            </StackPanel>
                            
                            <!-- 按钮区域 -->
                            <Grid Margin="0,10,0,20" HorizontalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="20"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 返回按钮 -->
                                <Button Grid.Column="0"
                                        Style="{StaticResource SecondaryButton}"
                                        Click="BackButton_Click"
                                        Width="120">
                                    <Button.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <ui:FontIcon Glyph="&#xE72B;" 
                                                FontSize="16" 
                                                Margin="0,0,8,0"/>
                                            <TextBlock Text="返回"/>
                                        </StackPanel>
                                    </Button.Content>
                                </Button>
                                
                                <!-- 注册按钮 -->
                                <Button Grid.Column="2"
                                        x:Name="RegisterButton"
                                        Content="注册" 
                                        Style="{StaticResource ModernButton}"
                                        Click="RegisterButton_Click"
                                        Width="120"/>
                            </Grid>
                        </StackPanel>
                    </TabItem>
                </TabControl>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>